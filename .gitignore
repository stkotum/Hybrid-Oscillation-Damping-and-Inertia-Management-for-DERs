# Large generated nonlinear data, written by the local nonlinear drivers
# (modified/runnable_notpublic/, called from run_smallexample_feng_paper)
case1_nonlinear.mat
case1_nonlinear_damping_comm.mat
case1_nonlinear_damping_comm_K*.mat

# Local safety backups (created when sweeping K)
*.bak_K*

# Background nonlinear sim logs
modified/sim_K*_nl.log
texas2000_*.log

# Simulink build artifacts
slprj/
*.slxc
*.slxc-info
*.autosave
*.asv
*.mexw64
*.mexa64
*.mexmaci64
cache/

# LaTeX build artifacts
*.aux
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
build.log
build2.log

# macOS
.DS_Store

# Obsolete linear cross-runner hand-off (no longer produced by any script)
modified/linear/cases/linear_results.mat

# Large-system sweep caches (regenerated by paper_figures/private/sweep_data; ~650 MB).
# Each result_share_*_K*.mat is a per-share linear sim that the script reruns
# automatically if missing; no need to keep them in git.
modified/paper_figures/_cache/

# third-party toolbox for PGF/TikZ export (matlab2tikz); not committed
modified/external/

# Internal development notes (kept locally, not part of the public repo).
# Only README files are tracked.
*.md
!README.md

# Nonpublic sim drivers: they wrap the upstream Simulink model and are kept local.
modified/runnable_notpublic/

# Paper repo (separate gitlab repo with its own .git) kept locally under papers/;
# not tracked by this code repo.
modified/papers/Renewable_Energy_Grid_Integration_Week/
